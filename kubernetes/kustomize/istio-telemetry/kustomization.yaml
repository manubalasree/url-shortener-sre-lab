apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: istio-system

resources:
  - telemetry.yaml

# Note: The configmap.yaml is commented out because it may conflict
# with existing Istio installation. If needed, apply manually or
# patch the existing istio configmap instead.
# - configmap.yaml

commonLabels:
  app.kubernetes.io/component: telemetry
  app.kubernetes.io/part-of: istio
